# 异步聚合交易数据获取器依赖包
# 在原有依赖基础上新增异步支持

# 原有依赖
pandas>=1.3.0
requests>=2.25.0

# 新增异步依赖
aiohttp>=3.8.0
asyncio  # Python 3.7+ 内置，无需单独安装

# 可选：性能监控和调试
aiofiles>=0.8.0  # 异步文件操作（如需要）
uvloop>=0.17.0   # 高性能事件循环（Linux/macOS） 