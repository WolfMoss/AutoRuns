# WebSocket实时数据订阅依赖包
# 币安WebSocket API客户端所需依赖

# 核心依赖
pandas>=1.3.0
websockets>=10.0
asyncio  # Python 3.7+ 内置

# 原有依赖（如果需要REST API功能）
requests>=2.25.0
aiohttp>=3.8.0

# 数据处理
numpy>=1.20.0
python-dateutil>=2.8.0

# 可选：性能优化
uvloop>=0.17.0   # 高性能事件循环（Linux/macOS）

# 安装命令：
# pip install -r requirements_websocket.txt 